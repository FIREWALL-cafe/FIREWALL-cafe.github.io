import React from 'react';
import { SearchBar, Translation, VotePanel, Results } from './';

const Layout = () => (
  <div>
    <SearchBar />
    <Translation />
    <VotePanel />
    <Results />
  </div>
)

export default Layout;
